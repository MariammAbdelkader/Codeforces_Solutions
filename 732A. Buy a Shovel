#include <iostream>

using namespace std;

int main()
{
    int k=0,r=0,c=0,result=0;
    cin>>k>>r;
    while(true)
    {
        c++;
        result=k*c;
        if(result % 10==0 || result % 10 ==r)break;
    }
    cout<<c;
    return 0;
}

